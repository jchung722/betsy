


<div class="row">
  <div class="column">
    <% if current_user %>
    <%= "#{@merchant.displayname}" %>



        <%= form_for Category do |f| %>
          <%= f.label "Category Name" %>
          <%= f.text_field :name %>
          <% f.submit %>
        <% end %>

    <% else %>
    <h2> You are not authorized to visit this page.  Please login as a merchant.</h2>

    <% end %>
  </div>
</div>
