<h1>What's in my cart?</h1>

<% if session[:order] %>

  <h2>Your cart contains:</h2>

  <% @order.orderitems.each do |orderitem| %>
    <p><%= "Product: #{Product.find(orderitem.product_id).name}, Quantity: #{orderitem.quantity}, Price per: #{Product.find(orderitem.product_id).price}" %></p>
  <% end %>

<% else %>

  <p>Your cart is empty! That makes the unicorns sad :(</p>

<% end %>
